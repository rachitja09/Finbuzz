[pytest]
minversion = 7.0
addopts = -q
python_files = test_*.py
# Prevent pytest from recursing into backup and virtualenv directories which may contain
# files with the same module names as the project's tests.
norecursedirs = backups .venv-1 .venv-2 .venv venv .git __pycache__
